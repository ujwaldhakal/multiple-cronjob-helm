apiVersion: v1
kind: Service
metadata:
  name: {{ include "multiple-cronjobs.fullname" . }}
  labels:
    {{- include "multiple-cronjobs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multiple-cronjobs.selectorLabels" . | nindent 4 }}
